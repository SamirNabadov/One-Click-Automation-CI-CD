spring:
  application:
    name: uni-transfer

server:
  port: 8888

management:
  security:
    enabled: false
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include:
          - health
  endpoint:
    health:
      probes:
        enabled: true
      enabled: true
  server:
    port: 8889

